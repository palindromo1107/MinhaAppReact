/*
{escolas.map((escola) =>
                    (<CardEstrutura id={escola.CO_ENTIDADE} escola={escola} />)
                )}
*/

function Main() {

    return (<></>
    )
}

export default Main